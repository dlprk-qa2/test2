apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fraud.fullname" . }}-test-connection"
  labels:
    {{- include "fraud.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fraud.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
