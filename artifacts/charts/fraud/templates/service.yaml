apiVersion: v1
kind: Service
metadata:
  name: {{ include "fraud.fullname" . }}
  labels:
    {{- include "fraud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fraud.selectorLabels" . | nindent 4 }}
